# Matomo MCP Server Plugin

## Configuration

Configure session lifetime in `config/config.ini.php`:

```ini
[McpServer]
session_ttl = 3600
```

If missing or invalid, a fallback of `3600` seconds is used.

## License

GPL v3 or later
