<?xml version="1.0"?>
<ruleset name="mcpserver" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="vendor/squizlabs/php_codesniffer/phpcs.xsd">
    <description>Matomo Coding Standard for McpServer plugin</description>

    <arg name="extensions" value="php" />

    <file>.</file>

    <exclude-pattern>*/vendor/*</exclude-pattern>

    <rule ref="Matomo"></rule>

    <rule ref="PSR1.Classes.ClassDeclaration.MultipleClasses">
        <!-- Allow using multiple classes in one file for tests -->
        <exclude-pattern>tests/*</exclude-pattern>
    </rule>
</ruleset>
